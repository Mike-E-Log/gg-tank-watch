# Runtime artifacts (regenerated on every writer tick)
status.json
status.json.tmp
updates.log
breaking_events.jsonl

# Restore points / autosaves
*.bak
*-autoplan-restore-*.md

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/

# IDE / agent harness state
.vscode/
.idea/
.claude/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini

# Eval transient output (the .jsonl log itself is kept)
eval/.last_run/
eval/summary.json

# Orchestration scratch (split-and-orchestrate wiki/state)
.orchestra/

# Build-loop / planning artifacts (regenerated per session)
loop/
plan/
prompts/

# Repo-glowup scratch: social-card drafts + render scripts. The final card is
# uploaded at GitHub Settings -> Social preview, not committed to the repo.
glowup/

# Generated task-list mirror (regenerated from the live Claude Code task list)
TASKS.md

# Secrets / local env (never commit)
.env
.env.local
.env.*.local
.vercel
.gstack/
.firecrawl/

# One-off scratch files (not part of the project)
render_safety_briefing.py
safety-briefing-philosophy.md
safety-briefing.png
Mobile _ 390.html

# Visual-QA scratch: Edge-headless capture tooling (_capture.*, _seed.html,
# _before_tmp.html, _verify_banner.py) and screenshot dumps (_shots/ anywhere)
_*
