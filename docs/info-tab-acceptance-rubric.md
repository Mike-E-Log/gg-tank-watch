# Info Tab — Acceptance Rubric (fixed target)

**Purpose.** This is the *fixed target* the Info tab is judged against, so passes **converge** instead of churning. It exists because the Info tab went 3→6→4 sub-tabs across PRs #108–#110 with the structural target explicitly deferred ("pending design decision") and a visual check that ran *after* shipping rather than *in* the loop — see `~/Documents/AI_Agent_UIUX_Research_20260602/` for the full diagnosis. **Do not start an Info-tab pass without this rubric open. If the target should change, change THIS FILE first, then build — never the reverse.**

**Scope note.** The Info tab is a *design-complete, frozen-archive* surface. This rubric is the **definition of done for any future change**, not a directive to restyle what shipped.

---

## 0. Hard constraints (any failure = pass fails, no exceptions)

These are the criteria a text-only test cannot see and that caused the #108→#109 reversal. **Verify by rendered screenshot, not by reading CSS.**

- [ ] **Sub-tab bar fits ONE row, NO horizontal scroll, at 320 / 360 / 375 / 390 / 430 px.** Every tab label fully visible (no clipped "Recovery", no hidden "About"). *(This is the exact failure of PR #108.)*
- [ ] **No content clipped or overflowing** its panel at those widths, light **and** dark.
- [ ] **Persistent honesty disclosure visible** (never clipped) at the shortest supported height (≈568px CSS).
- [ ] **Touch targets ≥ 44px** for every sub-tab and interactive row.
- [ ] **English-only** safety/UI copy (G1). No non-English surface, even a link framed as ours.
- [ ] **Conduit only** — no authored directives; route to officials.

## 1. Information architecture (the structural target — FIXED)

- [ ] Exactly **4 equal-width sub-tabs, single row**: `Summary | Officials | Resources | About` (`renderInfoTab`). Changing the count or set requires editing this file first.
- [ ] Each panel opens with one `.info-desc` descriptor band (3px celadon left border + surface tint).
- [ ] **Summary** = sourced peak facts only, **trimmed to fit mobile with no vertical scroll** (the panel's available height is ≈342px at 568px tall; measured by render 2026-06-03 the 9-row `.info-kv-row` set was 486px @375 / 541px @320, over by 144/199px). **Keep 7 rows, narrative order:** Substance · Facility · Peak tank temperature (≈100°F) · Peak evacuation (≈50,000) · Evacuation zone (≈9 sq mi) · Incident window · Outcome. **Cut 2:** Tank, Crack observed — cutting Crack also requires removing the now-dead `#info-crack-val` fill in `updateInfoData` (the green `<span class="yes">` at ~`dashboard.html:2167`); keep the `info.tankCrack`/`tankCrackY` keys (read by `test_info_disclosures` past-tense guard) or update that test. **Single-line these values** (they wrap to 2–3 lines at ≤320px): Evacuation zone → `~9 sq mi, 6 cities`; Facility → `GKN Aerospace`; Peak tank temperature → `~100°F (gauge max)`; Outcome → `No injuries; 0 displaced`. **Ranked fallback** if 320 still clips in the render loop: drop Peak tank temperature, then Incident window. Verify by render (eyes) at 320/375/390 + a geometry/invariant guard (not text-only). Decided in the planning gate 2026-06-03 per user.
- [ ] **Officials** = route-to-officials only: the official channels, each with a one-line `.info-row-desc` description of what the channel is for. The 3 channels render as **calm bordered cards** (design-review-locked 1A 2026-06-03): `--sa-surface` fill + a full `--sa-border` hairline (NOT a colored-left-border — that's an AI-slop tell, and celadon-left is reserved for `.info-desc`), ~14–16px padding, channel name 15px/600 at `--sa-text`, the `Official ↗` celadon cue, one-line `.info-row-desc` below; ≥44px tap target; ~3×~80px fills the 342px panel so routing-to-officials is the visual focus — Officials-only; this does **not** reintroduce the card grid cross-model review hard-rejected, which is *Resources*-scoped (target change requested 2026-06-03 per user). No general safety-advice note (the "No single source…" note was removed 2026-06-02 per user — generic boilerplate that didn't fit a frozen, resolved archive).
- [ ] **Resources** = shelters + school closures + recovery aid as labeled `.info-section-title` sections (no card grid — cross-model review hard-rejected card-grid-first), with the three sections **visibly delineated at a glance** (design-review-locked 2A 2026-06-03): generous top-space + a full-width `--sa-border` hairline rule above each `.info-section-title`, so the eye lands on three distinct blocks (Shelters / School closures / Recovery aid), not one undifferentiated list (reuses the existing hairline token; escalate to a subtle `--sa-surface` banded header only if the render isn't distinct enough; still no card grid). Descriptor band copy stays to **one line at 375px**. Rendered section titles read **Shelters / School closures / Recovery aid** — the `info.subtab.schools`/`info.subtab.recovery` values were lengthened from "Schools"/"Recovery" 2026-06-03 per user so each title names its section in full.
- [ ] **About** = AI disclosure (13px, body near-black `--sa-text` — binding honesty, **moved from the first line to the panel-closing line at the very bottom of About 2026-06-03 per user** (transparency tradeoff accepted: the persistent safety strip carries the load-bearing always-on honesty; the disclosure stays present and **legible at 13px, not shrunk to fine print** — position demoted, size not), **not** a gold accent; at the 13px primary-content scale shared with Summary/Officials/Resources rows, harmonized 2026-06-03 per user) + a **"Why this was made"** section (a `.about-why-title` group heading + one short resident-first paragraph as `.about-body` prose; conduit-true — it states the app gathered scattered official updates into one place and routed back to the officials in charge, authoring no directives; added in-app 2026-06-03 per user, overriding the earlier "methodology / who-made-it narrative lives in the README, not in-app" posture) + a **prominent, centered, tappable Accessibility link** (`.info-a11y-btn`: bordered, ≥44px tap target, centered below the Sources fold — made more apparent + more accessible 2026-06-03 per user; Terms lives in the persistent safety strip, so it is not duplicated here) + a "Sources" fold that is **open by default**, opens with a one-line `.info-fine` caption stating what the list is (the provenance trail the pipeline checked), and tags the official City/County sources (URL-derived: `ggcity.org`/`ocgov.com`, refresh-proof) with an "Official" label. The single duplicate routing line (`disclosure.aiRoute`) was removed 2026-06-02 — concrete 911/ggcity routing is carried persistently by the safety strip.

## 2. Visual system (token rubric — already stable in DESIGN.md; conform, don't reinvent)

- [ ] Type scale from `DESIGN.md` only: body Plus Jakarta Sans 14px/1.4; data IBM Plex Mono tabular-nums; labels 11–12px. **No inline `font-size`** (use `.info-fine` / `.info-desc` / `.info-who-body`; guarded by `test_no_inline_font_size_in_info_panels`).
- [ ] Surfaces `--sa-surface` on `--sa-bg`; `--sa-border` hairlines; celadon = active/interactive; gold reserved for the UNOFFICIAL pill (the AI disclosure now renders at body `--sa-text`, not gold — changed 2026-06-02 per user; the disclosure stays binding via its text + **legible 13px treatment**, not via color or first-line position — it moved to the bottom of About 2026-06-03 per user, with the persistent safety strip carrying the load-bearing always-on honesty).
- [ ] Spacing on the 4px base unit, compact density. Row padding consistent across panels (the #110 "breathing room" value, applied uniformly — verify rendered, not just the string).
- [ ] Sub-tab bar height **matches the News chip-bar** (verify the bars align at 375px by screenshot, not by `padding:` string match).

## 2a. Cross-panel consistency tokens (the four panels are ONE system — harmonized 2026-06-02)

Summary / Officials / Resources / About previously read as four designs (two key/value row systems, a 12px-vs-14px gutter drift, 500-vs-600 values, a lone dashed row, dimmer school names). Fixed tokens, guarded by `test_info_panel_consistency`:

- [ ] **One 14px horizontal gutter** for all panel content (`.info-section`, `.info-section-title`, `.info-kv-row`, `.info-schools-grid`, `.about-body`, `.info-who-body`) so every sub-tab shares one left edge. Nested `.info-section > .info-who-body` zeroes its inner gutter (no 14+14).
- [ ] **One key/value row**: `9px` vertical padding, `13px`, `1px solid var(--sa-border)` hairline, key `--sa-text-2`/400, value `--sa-text`/**600**. Applies to BOTH `.info-kv-row`/`.info-kv-val` (Summary) and `.info-row`/`.v` (Officials) — no dashed row, no 500 value.
- [ ] **List/link NAMES at value color `--sa-text`** (not dimmed): `.info-school-card`, `.shelter-name`, AND the Officials channel name (`.info-official-row .k`) — the official link name is primary content, so it reads as dark as the shelter names, not the muted key color (fixed 2026-06-03 per user). `9px` rhythm on list rows.
- [ ] **Primary content = 13px across all four panels** (Summary `.info-kv`, Officials `.info-row`, Resources `.shelter-name`/`.info-school-card`, About `.info-ai-disclosure` + `.source-item`) — harmonized 2026-06-03 per user (About's disclosure + source links were 12px outliers). **Role tokens shared by all panels:** descriptor band `.info-desc` 12.5px; **group/section titles 11px/700/caps/`--sa-text-2`** (Shelters/Schools/Recovery `.info-section-title` AND the About "Sources" toggle `.info-sources-toggle`, matched 2026-06-03 so a heading stands out above its 13px items); fine print 11px (`.info-fine`, `.info-row-desc`, sources caption); body prose `.info-who-body` 12px. The AI disclosure renders at body `--sa-text` (not a gold accent — changed 2026-06-02). About panel order (top → bottom), canonical: descriptor band → "Why this was made" → "Sources" fold → **Accessibility link** → **AI disclosure (panel-closing line)**. The Accessibility link is a **prominent, centered, bordered tappable link** (`.info-a11y-btn`, ≥44px tap target, celadon outline) — no longer the quiet 11px `.info-fine` footer link (made more apparent + accessible 2026-06-03 per user); the AI disclosure moved from the first line to the very bottom beneath it 2026-06-03 per user (stays 13px + legible — position demoted, size not).

## 3. Definition of done (the loop — run EVERY pass, in this order)

1. **Open this rubric.** If the change implies a structural edit (§1), amend §1 here *first*.
2. **Implement** against the fixed target.
3. **Render-and-diff IN the loop (not after):** open the page in the **native Claude Code + Chrome integration** (`/chrome`; real signed Chrome/Edge — clears SAC *and* the harness deny on raw `msedge.exe`, and renders the MapLibre map without the headless hang) at **320, 360, 375, 390, 430, 768 px**, light + dark. Compare each against §0 + §1 + §2; list differences; fix; re-shoot until zero §0 failures. *(The agent drives this via the Chrome tool; the user can also run `__qa_shots.ps1`. A headless Playwright `channel:'msedge'` test is the opt-in CI form — the agent cannot spawn a raw `msedge.exe`. **For the narrow widths, load the page in `__qa_harness.html?w=320|375|…` — the Chrome integration's `resize_window` clamps at ~412px innerWidth and can't reach true 320/375.** See `~/Documents/Eyes_In_The_Loop_Research_20260603/` and [[eyes-in-loop-stack]].)*
4. **Text eval** `python eval/run_all.py --skip integration` → green by scorecard (never `--quiet`).
5. **Add/keep a geometry guard** (see §4) so the §0 fit-failure class can't silently regress.
6. **SW cache bump** (`gg-tank-vN`) on any `dashboard.html`/config change.
7. Pass is "done" only when **§0 = all green by screenshot** AND text eval green.

## 4. Close the eval's blind spot (static floor in place; rendered guard optional)

The `eval/test_info_*` *text* guards are string-matches — they passed 171/171 while #108's bar was clipped at a real 375px. The shipped **static CSS-invariant** guard (`eval/test_info_subtab_fit.py`: equal-width `flex:1 1 0` + `min-width:0`, ≤4 tabs) now makes that overflow class **structurally impossible** — zero-dep, deterministic, the always-on floor. For a *rendered* check beyond the invariant, run a `getBoundingClientRect`/`scrollWidth` probe through the **native Chrome integration** (agent-mediated) or, for a headless exit-code CI gate, a scripted **Playwright `channel:'msedge'` (or `'chrome'`)** test that **fails when the sub-tab bar's scrollWidth exceeds its clientWidth at 375px**  (NOT `--dump-dom`, broken on `--headless=new`). This keeps the §0 fit-constraint an enforced behavioral gate — extending the project's scalable-oversight thesis to the visual layer.

---

*Companion: cross-project process in `~/.claude/agent-uiux-playbook.md`. Full evidence + diagnosis in `~/Documents/AI_Agent_UIUX_Research_20260602/`.*
